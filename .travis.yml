language: python

python:
  - 3.6
  - 3.7
  - 3.8

before_install:
  - python --version
  - pip install -U pip
  - make install

script: make ci # lint and test

after_success:
  - codecov # submit coverage
